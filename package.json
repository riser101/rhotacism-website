{
  "type": "module",
  "name": "rhotacism-website",
  "version": "1.0.0",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  },
  "scripts": {
    "test": "playwright test",
    "test:replay": "node build.js && playwright test --config=playwright.replay.config.js"
  }
}
